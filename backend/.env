# Base de datos PostgreSQL (lo que ya tenías)
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=123
PGDATABASE=proyectococode
PGPORT=5432

# Servidor (nuevo)
PORT=3001

# JWT para autenticación (nuevo - MUY IMPORTANTE)
JWT_SECRET=cocode_municipalidad_huehuetenango_2025_super_secreto_token
JWT_EXPIRES_IN=8h

# Entorno (nuevo)
NODE_ENV=development
API_URL=http://localhost:3001